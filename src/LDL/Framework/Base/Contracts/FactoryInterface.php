<?php declare(strict_types=1);

/**
 * This interface works just as a placeholder for other factories to extend to it
 * so the user can easily determine if an object is instanceof FactoryInterface
 * and then take specific action with that information.
 *
 * IMPORTANT: This interface must be NOT used by itself.
 */

namespace LDL\Framework\Base\Contracts;

interface FactoryInterface
{

}
